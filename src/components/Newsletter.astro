---
// Newsletter-Anmeldung Platzhalter
// Wird später mit echtem Newsletter-Service verbunden
---

<section class="bg-primary py-12">
  <div class="max-w-4xl mx-auto px-6 lg:px-12 text-center">
    <h3 class="text-2xl font-headline text-white uppercase mb-3">
      Bleib auf dem Laufenden
    </h3>
    <p class="text-white/80 mb-6 max-w-lg mx-auto">
      Erhalte Updates zu Veranstaltungen, neuen Ideen und dem Wahlkampf direkt in dein Postfach.
    </p>

    <div class="flex flex-col sm:flex-row gap-3 justify-center max-w-md mx-auto">
      <label for="newsletter-email" class="sr-only">E-Mail Adresse für Newsletter</label>
      <input
        id="newsletter-email"
        type="email"
        placeholder="Deine E-Mail-Adresse"
        disabled
        aria-disabled="true"
        aria-describedby="newsletter-status"
        class="flex-1 px-4 py-3 bg-white/20 text-white placeholder-white/60 border border-white/30 focus:outline-none focus:border-white cursor-not-allowed"
      />
      <button
        type="button"
        disabled
        aria-disabled="true"
        class="px-6 py-3 bg-white/30 text-white/60 font-headline uppercase text-sm tracking-wide cursor-not-allowed"
      >
        Anmelden
      </button>
    </div>

    <p id="newsletter-status" class="text-white/60 text-sm mt-4">
      <span role="img" aria-label="Briefkasten">📬</span> Newsletter-Anmeldung kommt bald!
    </p>
  </div>
</section>
